*** Settings ***
Documentation     NOTE:
...               add note here
Suite Setup       Setup_suite
Default Tags      win    unix
Test Template     Update Template
Resource          ../../resources/keywords.txt

*** Variables ***
${SPACE}          " "

*** Test Cases ***    URI                            PASSWORD        WORKING COPY                                      REVISION    OVERWRITE    RETCODE    STATUS
Valid Update Head with pserver
                      ${_CVS_URI}                    ${_CVS_PASS}    ${_WORKING_DIR}${/}unix_module                    ${EMPTY}    NO           0          ${ENDED_OK}

Valid Update Head with pserver Overwrite
                      ${_CVS_URI}                    ${_CVS_PASS}    ${_WORKING_DIR}${/}unix_module                    ${EMPTY}    YES          0          ${ENDED_OK}

Valid Update Revision with pserver
                      ${_CVS_URI}                    ${_CVS_PASS}    ${_WORKING_DIR}${/}unix_module                    1.1.1.1     NO           0          ${ENDED_OK}

Valid Update Revision with pserver Overwrite
                      ${_CVS_URI}                    ${_CVS_PASS}    ${_WORKING_DIR}${/}unix_module                    1.1.1.1     YES          0          ${ENDED_OK}

Valid Update contains spaces
                      ${SPACE}${_CVS_URI}${SPACE}    ${_CVS_PASS}    ${SPACE}${_WORKING_DIR}${/}unix_module${SPACE}    ${EMPTY}    YES          0          ${ENDED_OK}

*** Keywords ***
Update Template
    [Arguments]    ${uri}    ${password}    ${working_copy}    ${revision}    ${overwrite}    ${returnCode}
    ...    ${returnStatus}
    Init Action    ${ACTION_UPDATE}
    Action Set    ${PRT_UPDATE_CVS_URI}    ${uri}
    Action Set    ${PRT_UPDATE_CVS_PASSWORD}    ${password}
    Action Set    ${PRT_UPDATE_CVS_WORKING_DIRECTORY}    ${working_copy}
    Action Set    ${PRT_UPDATE_CVS_REVISION}    ${revision}
    Action Set    ${PRT_UPDATE_CVS_OVERWRITE}    ${overwrite}
    Action Execute
    Action Report Should Be Found
    Action Return Code Should Be    ${returnCode}
    Action Return Status Should Be    ${returnStatus}

Setup_suite
    [Documentation]    TODO: Write all steps need prepare before testing here
    Log    Start testing for CVS Update
